logging:
  level:
    it.adt.comparison: INFO
    it.adt.comparison.service: WARN

my-properties:
  name: Pino

# DATASOURCE
## H2 ##
spring:
  datasource:
    url: jdbc:h2:mem:testdb
    username: test
    password:
  # JPA - Hibernate
  jpa:
    hibernate:
      ddl-auto: update